@model WebClient.Models.ClientModel
@model WebClient.Models.UserModel
@model WebClient.Models.CredentilsModel


@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>ClientModel</h4>
        <hr />
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true)

            <fieldset>
                <legend>Client</legend>

                <div>
                    @Html.LabelFor(model => model.User.FirstName)
                </div>
                <div>
                    @Html.EditorFor(model => model.User.LastName)
                </div>
                <div>
                    @Html.EditorFor(model => model.User.Email)
                </div>
                <div>
                    @Html.EditorFor(model => model.Credentials.Login)
                </div>
                <div>
                    @Html.EditorFor(model => model.Credentials.Password)
                </div>
                <div>
                    @Html.EditorFor(model => model.Credentials.Login)
                </div>
                <div>
                    @Html.EditorFor(model => model.Credentials.Role)
                </div>

                <p>
                    <input id="saveButton" type="submit" value="Save" />
                </p>
            </fieldset>
        }

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
